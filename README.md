# coastal.nvim

`coastal.nvim` is the Neovim colorscheme for the broader **Coastal** theme ecosystem.

This repo contains the Neovim theme itself. It is intended to pair with the Omarchy theme repo:

- **Omarchy theme repo:** `omarchy-coastal-theme`
- **Neovim theme repo:** `coastal.nvim`

## What this repo is

This repo is focused on Neovim only.

It provides the editor colorscheme, highlight groups, and plugin styling needed for Neovim to match the rest of the Coastal theme.

## Relationship to Omarchy

`coastal.nvim` is the editor-side counterpart to `omarchy-coastal-theme`.

That means:

- `coastal.nvim` handles Neovim colors and highlights
- `omarchy-coastal-theme` handles desktop and app theming for Omarchy
- both are meant to share the same palette and visual direction

## Theme pairing

Use these together for a matching setup:

- **Neovim:** `coastal.nvim`
- **Omarchy:** `omarchy-coastal-theme`

## Goal

The goal of `coastal.nvim` is to make Neovim feel like a natural part of the wider Coastal environment instead of a separate, mismatched editor theme.
